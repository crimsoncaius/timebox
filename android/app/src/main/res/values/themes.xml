<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only covers the pre-Compose window background. -->
    <style name="Theme.Timebox" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/timebox_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
